# TODO

- [ ] Configuration menu (options page) for API key, vault directory, server port, default media type
- [x] Make extension permanent (install.bat adds registry key so Firefox loads it on every startup)
