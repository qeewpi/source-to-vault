# TODO

- [ ] Configuration menu (options page) for API key, vault directory, server port, default media type
- [x] Make extension permanent (install.bat adds registry key so Firefox loads it on every startup)
- [ ] Selective extract for quotes into created (or to be created) source note of the same page.
- [ ] Fix topic note creation: suggested topics appear as links but the notes are not actually created in the vault. Currently requires manually clicking the link, creating the note, and moving it to the topics folder.
